services:
  routes:
    - name: client-list
      prefix: '/client-list-api'
      target: 'http://localhost:4500'

  cors:
    - 'http://localhost:4000'

  exclusions:
    - '/favicon.ico'
    - '/'
